palabra_usuario = input('Ingresa una palabra o numero de 2 digitos diferentes: ') #Solicitamos al usuaio una palabra o numero de dos digitos diferentes.

palabra_usuario_invertido = palabra_usuario[::-1] #Invertimos lo ingresado por el usuario

print(f'La palabra inverida es:  {palabra_usuario_invertido}') #Iprimimos la palabra invertida